#!/bin/bash

# Check if solved.py exists in the current directory
if [[ -f "$(pwd)/solved.py" ]]; then
    python3 "$(pwd)/solved.py" "$@"
else
    echo "Error: Not in a valid Codeforces directory with solved.py."
    exit 1
fi

